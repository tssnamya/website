# Normalize line endings to LF in the repo (consistent across Windows/Linux/Vercel)
* text=auto eol=lf

# Treat binary assets as binary (never normalize)
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
*.db   binary
